# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ========================================
# PRODUCTION DEPLOYMENT INSTRUCTIONS
# ========================================
# 
# 1. NEVER commit .env.local to GitHub
# 2. For production deployment, set these as environment variables in your hosting provider:
#    - Vercel: Project Settings > Environment Variables
#    - Netlify: Site Settings > Environment Variables  
#    - Railway: Project > Variables
#    - Docker: --env-file or -e flags
#
# 3. Generate secure values for production:
#    - Use strong, unique passwords
#    - Generate random tokens (use: openssl rand -hex 32)
#    - Use production database URLs
#

# ========================================
# ADMIN PANEL CREDENTIALS
# ========================================
# Main admin panel login credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=YourSecurePassword123!

# Admin2 default credentials (can be changed via admin panel)
DEFAULT_ADMIN2_USERNAME=admin2
DEFAULT_ADMIN2_PASSWORD=Admin2SecurePass456!

# ========================================
# SECURITY KEYS
# ========================================
# Generate with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_key_here_at_least_32_characters_long
ENCRYPTION_KEY=your_encryption_key_here_32_chars
ADMIN_API_TOKEN=secure_admin_api_token_2024

# ========================================
# FILE UPLOAD SETTINGS
# ========================================
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/webp,image/gif

# ========================================
# RATE LIMITING
# ========================================
MAX_REQUESTS_PER_MINUTE=60
MAX_UPLOAD_REQUESTS_PER_MINUTE=10

# ========================================
# DATABASE (if using external DB in future)
# ========================================
# DATABASE_URL=postgresql://username:password@hostname:port/database
# REDIS_URL=redis://username:password@hostname:port

# ========================================
# EXTERNAL SERVICES (if needed)
# ========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ========================================
# PRODUCTION ONLY
# ========================================
# Set to 'production' in live environment
NODE_ENV=development
